GCP_PROJECT_ID=gcp-rag-poc-490814
GCP_REGION=europe-west1

# PostgreSQL (local Cloud SQL substitute)
POSTGRES_USER=rag
POSTGRES_PASSWORD=rag
POSTGRES_DB=regional
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# fake-gcs-server (local GCS / Box substitute)
GCS_EMULATOR_HOST=http://localhost:4443
GCS_DOCUMENTS_BUCKET=rag-poc-documents-dev

# BigQuery emulator
BIGQUERY_EMULATOR_HOST=http://localhost:9050
BIGQUERY_DATASET=global_metrics

# GCP — Cloud SQL (real)
CLOUD_SQL_HOST=34.76.9.33
CLOUD_SQL_PORT=5432
CLOUD_SQL_USER=rag
CLOUD_SQL_PASSWORD=change-me
CLOUD_SQL_DB=regional

# GCP — GCS (real)
GCS_BUCKET=gcp-rag-poc-490814-documents-dev
