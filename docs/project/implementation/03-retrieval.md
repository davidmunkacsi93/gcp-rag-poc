# Phase 03 — Retrieval

Goal: a query is routed to the correct retrieval paths — semantic search against the Vector Store and NL-to-SQL against BigQuery and Cloud SQL — with results fused and re-ranked.

> To be planned.
